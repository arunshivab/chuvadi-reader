@*
    The C·ச·huvadi wordmark lockup. In production this becomes your real
    BrandMark / LiPicon-backed lockup; here it is pure markup + tokens.
*@
<span class="cv-brand" style="font-size:@(Size)px">C<span class="cv-brand-ta">ச</span>huvadi</span>

@code {
    [Parameter] public int Size { get; set; } = 21;
}
